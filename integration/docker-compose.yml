version: "3.1"

services:
  generator:
    image: my-registry:55000/gen:ci-9
    # ports:
    #   - "8090:80"
  mail:
    image: mailhog/mailhog
    # ports:
    #   - "8025:8025"
  integration:
    image: my-registry:55000/gen:integration-9
    build: .
